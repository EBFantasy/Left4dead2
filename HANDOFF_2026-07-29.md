# 交接文档 — SCAR 右键推挤 / 平滑后坐力

日期：2026-07-29
分支：`arena/019fa31c-left4dead2`
最后提交：`06da110`
最后测试版本：`punch 0.9.8-punch` + `SCAR shove-fix v9 keep-forced-bit`

写给接手的人（或未来的自己）。**两个问题都未解决。** 本文档的目的是让你不用重走我走过的九轮弯路，直接从正确的层级动手。

---

## 一、当前状态（一句话）

| 问题 | 状态 | 结论 |
|---|---|---|
| SCAR 右键推挤失效 | **未解决** | 纯 VScript 很可能做不到，建议转 SourceMod |
| ADS 后坐力消失 | **回归，已定位** | 是我最后一版改错，有明确回退方案（见 §4.1） |
| 腰射后坐力 | 可用 | 唯一能正常工作的部分 |

---

## 二、SCAR 右键推挤

### 2.1 现象

全自动模式下按右键推挤，大部分时候没有任何反应。

### 2.2 最新一轮日志的硬数字

```
真人右键被脚本看到      60+ 次
推挤真正执行             2 次      ← 判据：m_flNextSecondaryAttack 出现 0.766s 冷却
射击模式                FULL AUTO  ← 已确认，不是 burst
构建标识                shove-fix v9  ← 确认测的是最新包
```

**关键否定证据：**

```
yield   (v8 新增分支)  出现 0 次
collide (v8 新增诊断)  出现 0 次
```

`yield` / `collide` 位于 `Process shots` 段，条件是「玩家按住右键的同时脚本要合成推挤」。**它一次都没触发**，说明：

> 真人按键与脚本合成推挤**并不在同一 tick 相遇**。
> 我在 v8/v9 押注的「合成推挤吞掉真人按键」这条假设 —— **已被排除**。

### 2.3 已排除的原因（不要再查这些）

| # | 假设 | 排除依据 |
|---|---|---|
| 1 | 事件回调没注册 | 改用弹匣轮询后开火检测正常 |
| 2 | `.find()` 索引 0 假值 bug | 已修（`Shooter.find()` 改 `!= null`），行为无变化 |
| 3 | `foreach` + `remove` 跳元素 | 已改倒序 `for`，行为无变化 |
| 4 | Observe 段 100 秒冻结 | v7 改为只停 primary，日志确认冻结归零，推挤仍失效 |
| 5 | `m_flNextAttack` 总闸 | v7 每 tick 主动拉回，日志确认闸被打开，推挤仍失效 |
| 6 | `m_iShovePenalty` | v7 主动清零，日志确认清零 3 次，推挤仍失效 |
| 7 | `m_afButtonDisabled` | 全仓库无人写入，日志中从未作为 blocker 出现 |
| 8 | 射击模式是 burst | **错误假设，我曾据此与用户争执。** 日志有连续 14 发记录，burst 打不出来 |
| 9 | 合成推挤吞掉真人按键 | v8/v9 已处理，`yield`/`collide` 计数为 0，**从未发生** |

### 2.4 为什么 VScript 可能根本做不到

引擎里推挤的判定过程（`hl2sdk` / `TF2-Base` 已核对）：

```cpp
// player_command.cpp — CPlayerMove::RunCommand()
ucmd->buttons |= player->m_afButtonForced;    // ① 先 OR 强制位
ucmd->buttons &= ~player->m_afButtonDisabled;

// baseplayer_shared.cpp — CBasePlayer::UpdateButtonState()
m_nButtons = nUserCmdButtonMask;              // ② 再赋值给脚本可读的变量

// baseplayer_shared.cpp — CBasePlayer::ItemPostFrame()
if (curtime < m_flNextAttack) { weapon->ItemBusyFrame(); }   // ③ 总闸
else                          { weapon->ItemPostFrame(); }

// basecombatweapon_shared.cpp — CBaseCombatWeapon::ItemPostFrame()
if ((pOwner->m_nButtons & IN_ATTACK2) && CanPerformSecondaryAttack())  // ④ 消费
```

**问题的本质：**

- ①②③④ 全部发生在**同一帧内部**，是引擎的一条连续调用链
- VScript 只能在 **tick 边界**读写网络变量，**看不到也插不进这条链**
- 脚本在 tick 边界把闸打开，等下一帧引擎跑到 ④ 时，中间可能已被别的代码改回去；而脚本无法在 ④ 之前的瞬间介入

这是**层级不匹配**，不是逻辑写错。九轮修改都在 tick 边界打转，所以看起来"每次都找到原因、每次都没解决"。

### 2.5 建议的正确做法（SourceMod）

```cpp
public Action OnPlayerRunCmd(int client, int &buttons, ...)
{
    // 这里位于上面 ①②③④ 之前，是唯一能可靠介入的位置
    if (buttons & IN_ATTACK2) {
        // 确保这一帧引擎一定能消费掉推挤
        SetEntPropFloat(weapon, Prop_Send, "m_flNextSecondaryAttack", GetGameTime());
        SetEntPropFloat(client, Prop_Send, "m_flNextAttack",          GetGameTime());
        SetEntProp(client, Prop_Send, "m_iShovePenalty", 0);
    }
    return Plugin_Continue;
}
```

`OnPlayerRunCmd` 在服务端预测之前拿到 usercmd，可以直接改按键位和时序，这正是本问题需要的层级。

**给 SourceMod 实现者的提示：** SCAR 脚本（`greenyoshiyt_scar.nut`）在**每一发自动射击**都会写 `m_afButtonForced |= 2048` 来驱动推挤动画。SourceMod 侧要注意区分这个合成位和玩家真实按键，否则会重复触发。区分方法见 §5 的 `ScarAutoSyntheticShove` 令牌。

---

## 三、后坐力

### 3.1 可用的部分：腰射

纯 VScript 写 `localdata.m_Local.m_vecPunchAngle`，**不碰视角**，因此不抢鼠标（用户已确认抢鼠标问题消失）。

引擎每 tick 自动回正（`gamemovement.cpp` → `DecayPunchAngle`）：

```cpp
m_vecPunchAngle    += m_vecPunchAngleVel * frametime
m_vecPunchAngleVel *= 1 - (9.0 * frametime)          // PUNCH_DAMPING
m_vecPunchAngleVel -= m_vecPunchAngle * min(65.0*frametime, 2.0)   // PUNCH_SPRING_CONSTANT
```

### 3.2 三条已验证的硬性限制

**(1) 服务端写 `m_vecPunchAngleVel` 无效**
被客户端预测丢弃。日志实证：vel 累积到 -512 而角度纹丝不动。**只能写 ANGLE。**

**(2) 回正速度不可调**
`PUNCH_DAMPING 9.0` / `PUNCH_SPRING_CONSTANT 65.0` 编译在引擎里。实测：AWP(5.75°) 和 Scout(3.35°) 回到 10% 都是 **283ms** —— 回正快慢与后坐力大小**无关**，所以「加大后坐力让它显得慢一点」这条路走不通。

绕过办法（已实现，`HOLD_FRAMES`/`HOLD_FRACTION`）：每帧把弹簧刚拿走的量还回去一部分，可把 AWP 回正拉长到 467ms。**注意必须让这个"补偿"平滑渐隐到零**，否则补偿停止的那一帧会产生 4 倍速度突变，体感就是"顿一下才开始回正"（我踩过，v0.9.6 的 bug）。

**(3) 连发峰值由弹簧平衡点决定，不是被上限截断**
连发时视角停在「每发推力 = 弹簧拉力」的平衡点。实测 SCAR 约 3.7°(ADS) / 6.1°(腰射)，而 `MAX_PITCH` 是 24 —— **上限从来没被碰到过**。想抬高峰值只能加大每发力度，或加一个随连发增长的额外项。

### 3.3 ADS 后坐力：完整的失败链条

这是**必须看懂的一节**，否则接手的人会重复我的错误。

**ADS 插件（`ads_base.nut:1258-1266`）的后坐力代码：**

```squirrel
local PunchAngle = NetProps.GetPropVector(player, "localdata.m_Local.m_vecPunchAngle");
local gunRecoil  = PunchAngle.x - attack.last_recoil.x;
gunRecoil *= RecoilFactor;                              // RecoilFactor = 0.5
local Recoil = Vector(attack.last_recoil.x + gunRecoil, PunchAngle.y, PunchAngle.z);
NetProps.SetPropVector(player, "localdata.m_Local.m_vecPunchAngle", Recoil);
```

**这段代码只【缩放】后坐力，不【产生】后坐力。** `gunRecoil` 是差值；若无人写入 punch angle，差值为 0，它就什么也不做。

**问题 A：弹道成团（用户提供的弹孔照片）**

开镜连射时弹孔挤成三团，团间大跳。日志聚类验证：

```
第1团  7发   -3.80 ~ -7.80°     ← 七发挤在 4 度内
...
第4团  1发   -13.82°            ← 跳 3.48°
第5团  1发   -17.13°            ← 跳 3.31°
```

**原因：两个写入者抢同一个变量。** ADS 插件在**动画回调**里改写，而 smooth_recoil 逐发写入。动画跟不上射速时，我们的上跳被反复拽回陈旧的 `last_recoil`（→ 一团）；等回调对上账，一次性灌入累积差额（→ 团间大跳）。

**问题 B：我的错误修法（v0.9.8，当前状态）**

我推断「ADS 插件有自己的后坐力，让开就好」，于是加了 `ADS_HANDS_OFF` —— 开镜时脚本完全不写 punch。

**这个推断是错的**，因为上面说过：ADS 插件不产生后坐力，只缩放。脚本一撤出，`gunRecoil` 恒为 0，ADS 下就只剩游戏原生的微弱 punch。**这就是用户报告的"ADS 完全没有后坐力表现"。**

---

## 四、下一步怎么做

### 4.1 立刻恢复 ADS 后坐力（一行）

`smooth_recoil/scripts/vscripts/smooth_recoil/smooth_recoil_punch.nut` 第 287 行：

```squirrel
::SmoothRecoilPunch.rawset("ADS_HANDS_OFF", true);   // 改成 false
```

改为 `false` 即可回到 v0.9.7 行为：ADS 有后坐力，但**弹道成团问题会回来**。这是目前两害相权的取舍。

### 4.2 真正解决 ADS 成团的两条路

**路线一（纯 VScript，可行性中等）**
让 ADS 插件不再参与后坐力，由 smooth_recoil 独占：

1. `ads_base.nut` 第 239 行 `RecoilFactor = 0.5` → 改为 `1.0`
   （`RecoilFactor == 1` 时上面那段 `if` 整个不执行，插件彻底不碰 punch）
2. 同步改 `ads_base.nut` 第 3501 行的配置模板，否则 `LoadConfig()` 会用 `lxc/ads_base/settings.txt` 里的旧值覆盖
3. **删除玩家硬盘上已有的 `lxc/ads_base/settings.txt`**，或手动改其中的 `RecoilFactor`
4. 把 `ADS_HANDS_OFF` 设为 `false`
5. 在 smooth_recoil 里用 `IsAdsActive()` 判断，给 ADS 一个更小的 kick 系数（模拟原本 0.5 的手感）

这样只剩一个写入者，理论上成团消失。**未经测试。**

**路线二（SourceMod，最可靠）**
后坐力和推挤都在 SourceMod 里做，卸载 ADS 插件的后坐力部分。`OnPlayerRunCmd` 可以直接改 usercmd 角度，能实现完全自定义的回正曲线（VScript 做不到，见 §3.2(2)）。

---

## 五、代码地图

### smooth_recoil/

```
scripts/vscripts/
  mapspawn_addon.nut              ::SR_MODE 开关 + ::SR_LoadRecoil 加载器
  director_base_addon.nut         同一加载器的第二入口（两处都会调，已确认只加载一次）
  smooth_recoil/
    smooth_recoil_punch.nut       主力实现（v0.9.8）
    smooth_recoil_core.nut        旧版 SnapEyeAngles 实现，会抢鼠标，仅留作对比，勿用
console.log                       用户每轮上传的测试日志
FEASIBILITY_VERDICT.md            早期可行性结论（部分结论已被本文档修正）
```

**`smooth_recoil_punch.nut` 关键参数：**

| 参数 | 值 | 说明 |
|---|---|---|
| `VEL_ASSIST` | 0.0 | **务必保持 0**。非 0 会导致 vel 饱和在钳位值，引发不可预测的猛跳（踩过两次） |
| `ADS_HANDS_OFF` | true | 见 §4.1，**这是 ADS 无后坐力的开关** |
| `CLIMB_HIP` | 0.0 | 腰射不加爬升（用户明确要求腰射不动） |
| `MAX_STEP_FACTOR` | 1.45 | 单帧异常上限，按武器基础后坐力的倍数（不能用固定度数，会抹平喷射递增） |
| `HOLD_FRAMES` / `HOLD_FRACTION` | 36 / 0.90 | 单发武器慢回正，仅 awp/scout/chrome_shotgun/pumpshotgun |
| `SOFT_START` / `MAX_PITCH` | 13 / 24 | 软上限；实际很少触及（见 §3.2(3)） |

### l4d2advancing/scar_ads_compat_3681231504_3576501343/

```
scripts/vscripts/
  greenyoshiyt_scar.nut     SCAR 主脚本，推挤问题所在
  ads_base.nut              ADS 插件（167KB），后坐力缩放在 1258 行
  director_base_addon.nut   第 9 行有构建标识，用于确认玩家装的是哪版
```

**`greenyoshiyt_scar.nut` 关键位置：**

| 行号 | 内容 |
|---|---|
| ~14 | `::ScarAutoSyntheticShove` 令牌，区分合成推挤与真人按键 |
| ~30 | `::ScarDbg` / `::ScarDbgOnce` / `::ScarDbgShoveGates` 诊断工具 |
| ~289 | `logic_timer` RefireTime = 0.01（Think 驱动源） |
| ~302 | Observe 段（处理 burst 节奏） |
| ~465 | PlayerAttack 段（推挤动画 + 闸门操作） |
| ~595 | Process shots 段（合成推挤，v8 的 yield 分支在此，**从未执行**） |
| ~668 | Player Think 段（每 tick 跑，v7 的兜底开闸在此） |

**诊断开关：** 控制台 `script ScarShoveDebug <- false` 可关闭。上限 400 条（`ScarDbgOnce` 有分键限流）。

---

## 六、给接手者的几条忠告

1. **`_shotLogs < 30` 是全局上限，不是每武器。** 本轮日志 30 条在第 3047 行就用尽，而日志有 4520 行 —— 用户测 ADS 的部分完全没被记录。**要验证 ADS 行为，先把这个上限调大**（`smooth_recoil_punch.nut` 第 571 和 740 行，**两处都要改**）。我因为这个盲区做了错误推断。

2. **不要用「某个日志没出现」当作否定证据**，除非你确认那行日志的代码路径真的存在且未被限流。我曾因为 v5 自己删掉了一行日志，反推出「用户在 burst 模式」的错误结论，并据此与用户争执 —— 而实际日志里有连续 14 发的记录，burst 根本打不出来。

3. **Squirrel 陷阱**（都踩过）：
   - `array.find()` 返回索引，**索引 0 是假值** → 必须 `!= null`
   - `foreach` 中 `remove(i)` 会跳过下一个元素 → 用倒序 `for`
   - `return` 单独一行返回 `null`，值必须与 `return` 同行
   - `peak<-4.0` 会被解析成新槽操作符，要写 `peak < -4.0`
   - 同作用域不能重复 `local` 同名变量

4. **静态检查方法**（沙箱无网络，`gh` CLI 可用）：
   ```bash
   cd /tmp && gh api "repos/albertodemichelis/squirrel/tarball/master" > sq.tar.gz \
     && mkdir -p sqsrc && tar xzf sq.tar.gz -C sqsrc --strip-components=1 \
     && cd sqsrc && make -j4          # 产出 /tmp/sqsrc/bin/sq
   /tmp/sqsrc/bin/sq -c -o /tmp/o.cnut 目标文件.nut    # 无输出 = 通过
   ```
   注意 L4D2 支持标准 Squirrel 不支持的 `::a.b.c <- v` 语法，`aba_spitter_response.nut` 第 900 行会报错，**原始文件也报同样的错**，非新引入。

5. **改 `ads_base.nut` 的配置项时，必须同时改三处**，否则改动会被静默覆盖：
   - 第 239 行附近的运行时默认值
   - 第 3505 行附近的 `ReleaseDefaultSettings()` 模板
   - 玩家硬盘上已存在的 `lxc/ads_base/settings.txt`（`LoadConfig()` 会用它覆盖前两者）

---

## 七、诚实的结论

SCAR 推挤我改了九轮没解决。每一轮都找到了一个真实存在的缺陷并修好了它（`.find()` bug、遍历跳元素、100 秒总闸、闸门不重开、强制位误清除），日志也确实显示这些缺陷逐个消失 —— 但推挤始终没恢复。

回头看，问题在于**这些都是 tick 边界能观察到的东西，而推挤的消费发生在引擎单帧调用链内部，VScript 看不到那里**。继续在 VScript 里试错的期望收益很低。

建议：**推挤转 SourceMod**（§2.5），**ADS 后坐力先按 §4.1 一行回退恢复可用**，长期按 §4.2 路线一或二处理。
